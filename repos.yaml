repos:
  - id: /.*/
    workflow: default
    allowed_overrides: [workflow]
    allow_custom_workflows: true

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply